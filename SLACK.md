# 如何使用 Slack `#investment` 頻道

這份文件說明 `#investment` 頻道怎麼用,以及它跟這個 repo 的關係。

## 頻道是什麼

`#investment` 是一個 **private 頻道**,裡面有 Claude bot。日常的投資討論在這裡進行,
而需要「跑程式」或「改 code」的部分,由 Claude 接手寫進這個 repo。

簡單說:**Slack 是輸入,repo 是產出。**

## 基本用法:tag Claude

在頻道裡 `@Claude` 加上你的問題或指令,Claude 會**在 thread 裡回覆**。

```
@Claude 幫我看一下這季財報的重點
```

幾個要點:

- **一定要 tag**。沒有 tag 的訊息 Claude 不會回應 — 這是刻意的,你們可以正常聊天而不會被打擾。
- **回覆會開 thread**。主頻道保持乾淨,細節留在 thread 裡。
- **在 thread 裡繼續問就會延續同一段對話**,Claude 記得前面的脈絡,不用重複交代背景。
- **開新的主頻道訊息 = 開新對話**,不會帶到先前的上下文。

## 如果 Claude 沒有回應

tag 了之後 thread 裡一片安靜。這個失敗是**靜默的**:沒有錯誤訊息,
thread 裡不留任何痕跡,只是沒有人回你。

按順序檢查:

**1. Claude 有沒有在頻道裡?(先查這個)**

這是最常見的原因。`#investment` 是 private channel,**app 必須被明確邀請才會進來** —
tag 一個不在頻道裡的 app,訊息不會送達任何地方,自然也不會有回應。

在頻道裡輸入 `/invite @Claude`,或從頻道名稱 → 整合 → 加入 App。
成功的話會看到「Claude 已加入頻道」的系統訊息 — **看到那則才算數**。

判斷方法:如果**每一次** tag 都沒有回應,幾乎可以確定是這個原因。
app 沒加入是 100% 失敗,不會時好時壞。

**2. 有沒有真的 tag 到?**

純文字打 "Claude" 沒有用,要是藍色的 mention。

**3. 以上都對才考慮重試。**

零星失敗才是觸發不穩,在同一 thread 再 tag 一次即可。

不要繞道去別的地方請 Claude「幫忙補發到 Slack」 — 那條路徑是外部程式代你發言,
每次都要過權限核准,答案還得跨系統搬回來,而且**完全繞不過第 1 點** —
app 沒加入頻道,問題就還在。

## 兩條路徑的分工

搞混這兩者是多數摩擦的來源:

| 你想做的事 | 走哪裡 | 需要核准嗎 |
|---|---|---|
| 提問、丟圖分析、討論 | **Slack 裡 @Claude** | 不用 |
| 要改 repo 的程式碼 | Claude Code session | 不用(但由你決定要不要合併 PR) |
| 從外部代發訊息到頻道 | Slack MCP(側向) | **每次都要** |

第三種是例外路徑,日常用不到。會需要它,通常代表前兩條走岔了。

## 三種常見用法

### 1. 丟圖片請它分析

直接把截圖(K 線、財報表格、新聞頁面)貼上來,tag Claude 並說你想看什麼:

```
@Claude 分析這兩張圖片
```

圖片和訊息一起送出即可,Claude 讀得到附件。

### 2. 一般提問與研究

```
@Claude 這家公司的營收結構是怎樣?
@Claude 幫我整理這篇新聞的重點
```

適合快速釐清、彙整資訊。回答留在 thread 裡,之後可以回頭翻。

### 3. 請它改 repo(重點)

這是 `#investment` 跟一般頻道最不一樣的地方 — Claude 接上了這個 repo,
你可以直接請它寫程式:

```
@Claude 幫我寫一支抓每日收盤價的腳本
@Claude 更新 README
```

它會做的事:

1. 開一個 Claude Code session,把 repo clone 下來
2. 在**新分支**上開發(不會直接動 `master`)
3. commit 並 push
4. 給你一個開 PR 的連結,你自己決定要不要合併

**你永遠保有最後決定權** — Claude 不會自己把東西合進 `master`。

## 實務建議

- **指令講清楚一點比較省事**。「幫我寫個抓股價的腳本」跟「幫我寫一支腳本,抓台股上市
  公司每日收盤價,存成 CSV」,後者一次到位的機率高很多。
- **不確定的時候它會反問**。與其讓它猜,不如回答一下,少來回幾輪。
- **改 code 的請求盡量開新 thread**,跟研究討論分開,之後比較好找。

## 安全注意事項

- **不要在頻道裡貼 API 金鑰、密碼、券商帳號等憑證**。訊息內容會進到 Claude 的
  處理流程,也可能被寫進 repo。需要金鑰時走環境變數。
- 頻道是 private,但仍請以「會被留存」的前提發言。
- 這裡的討論與產出僅供內部研究參考,**不構成投資建議**。

## 相關連結

- 頻道:[#investment](https://w1732101547-am2187181.slack.com/archives/C0BM7QQAJLD)
- 這個 repo 的用途說明:[README.md](./README.md)
